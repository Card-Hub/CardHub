<!-- <script setup lang="ts">
import eventBus from 'path/to/eventBus'; // Adjust the path to your event bus file

const sendMessage = () => {
  const message = eventBus.getMessage();
  // Process the message or emit an event if needed
};
</script>

<template>
    <form @submit.prevent="sendMessage">
      <input v-model="message" type="text" />
      <button type="submit">Send</button>
    </form>
</template> -->
